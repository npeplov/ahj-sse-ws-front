1. сервер - на базе пакета ws с лекции
v 2. как серверу получить то что отправил юзер
3. как сделать чтоб было больше 1 юзера

4. Что должен отправлять сервер в ответ на сабмит формы:
  - фронт: юзернейм ->
  - back: 1. перебор объекта класса юзер. [...users].some()
          2. вернуть true ->
          - фронт: сообщение - выбери другое
          3. вернуть false и список юзеров, список сообщений ->
          фронт: отрисовать чат:
          - из response список юзеров
          - из response список сообщений
---
1. выводить отправленное сообщение в конверсейшн
2. scroll внутри дива - дз про чат
3. Список участников чата
---
на бэке класс хранит юзеров
1. если такой юзер есть -> сообщить чтобы выбрал другое

2. если закрыл вкладку - на сервер приходит ws.on('close'):
  - удалить юзера из массива
  - как определить кто отключился
  [...wsServer.clients]



3. если снова открыл - снова логиниться.

4. Получаем массив юзеров. Если user.you == true:
  - то nickname в template заменяется на you

посмотреть лекцию
читать документацию https://www.npmjs.com/package/ws
https://developer.mozilla.org/ru/docs/Web/API/WebSockets_API/Writing_WebSocket_client_applications
посмотреть у других студентов

on.connect - вернуть хэш.
юзер запишет в this.hash

отправляем сообщение:
Получаем массив сообщений. Если user.hash == this.hash:
  - то nickname в template заменяется на you